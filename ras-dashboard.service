[Unit]
Description=1C RAS Dashboard
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/ras-dashboard
ExecStart=/opt/ras-dashboard/venv/bin/python -m uvicorn main:app --host 0.0.0.0 --port 8080
Restart=on-failure
User=root

[Install]
WantedBy=multi-user.target
